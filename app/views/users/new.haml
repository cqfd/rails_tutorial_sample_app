%h1 Sign up

= form_for @user do |f|
  = render 'shared/error_messages', :object => f.object
  = render 'fields', :f => f
  .actions
    = f.submit "Sign up!"

%p 
  Already have an account?
  = link_to 'Sign in!', signin_path
